#ifndef lookinFile_h
#define lookinFile_h

/* function prototype - provides then name of the function, 
   the number of arguments given to the function, and the 
   type of each argument. It also provides the return type
   of the function. */

void count(char* filename, char* pattern);

#endif

